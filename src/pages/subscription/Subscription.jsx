import React from 'react';
import HelmetTitle from '../../shared/HelmetTitle';

const Subscription = () => {
    return (
        <div className='my-10'>
            <HelmetTitle title="Subscription || Trendify"></HelmetTitle>
            <h2 className='text-3xl font-bold'>this is subscrition page.</h2>
        </div>
    );
};

export default Subscription;